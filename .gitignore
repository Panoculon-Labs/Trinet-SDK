# Public repo: Android SDK as a prebuilt AAR (aar/) + docs, iOS SDK as Swift
# source (ios/) consumed via SwiftPM. Demo apps ship as GitHub Releases.
releases/
.DS_Store

# ── iOS / Xcode / SwiftPM ──
.build/
.swiftpm/
DerivedData/
Build/
# The iOS demo app's Xcode project is generated by XcodeGen
# (run `xcodegen generate` in ios/TrinetApp/). Never commit the pbxproj.
*.xcodeproj/
xcuserdata/
*.xcuserstate
Packages/
Package.resolved
